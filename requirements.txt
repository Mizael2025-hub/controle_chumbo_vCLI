# ============================================================
# Plataforma PCP (Módulo: Controle de Chumbo) — requirements
# Versões fixadas para reprodutibilidade (PRD §1.3 / AGENTS.md)
# ============================================================

# --- Core / Web ---
Django==6.0.6
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.2

# --- Settings / env ---
django-environ==0.14.0

# --- Database (PostgreSQL 16+) ---
psycopg[binary]==3.3.4

# --- Filas / Tarefas assíncronas (Celery + RabbitMQ + Redis) ---
celery==5.6.3
django-celery-beat==2.9.0
django-celery-results==2.6.0
dj-celery-panel==0.4.0
redis==8.0.1

# --- Staticfiles (prod) / WSGI ---
whitenoise==6.12.0
gunicorn==26.0.0

# --- IA (orquestração) ---
langchain==1.3.11
langgraph==1.2.7

# --- PDF (relatórios) ---
reportlab==5.0.0

# --- Dev / seeds (pt-BR) ---
Faker==40.28.1
